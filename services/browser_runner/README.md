# voyagent-browser-runner

Playwright-based worker that executes browser-automation drivers (e.g. VFS
visa portals). Isolated from the agent runtime so browser sessions can be
pooled, sandboxed, and restarted independently.

Status: skeleton — not implemented.
