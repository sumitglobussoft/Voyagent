// @voyagent/core — canonical domain types.
//
// The real types live in ./generated.ts and are produced by the codegen
// pipeline (see ../README.md). Keep this entry point intentionally small so
// that TypeScript tooling has a single file to re-export from.

export * from "./generated.js";
export type * from "./generated.js";
