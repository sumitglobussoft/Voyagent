/**
 * Test-runner setup — executed once before every Vitest run. Pulls in the
 * jest-dom matchers (`toBeInTheDocument`, `toHaveClass`, etc.) so we don't
 * have to import them per file.
 */
import "@testing-library/jest-dom/vitest";
