/// <reference types="vite/client" />

/**
 * Build-time defines injected by `vite.config.ts` (see `define:` block).
 * The Settings > About panel reads these; keep both declarations in sync
 * with the `define` keys.
 */
declare const __APP_VERSION__: string;
declare const __BUILD_DATE__: string;
