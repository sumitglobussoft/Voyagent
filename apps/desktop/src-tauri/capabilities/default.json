{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Voyagent desktop shell. Enables the main window plus scoped shell + dialog + auth + update plugins. Filesystem is intentionally not broadly permitted in v0 — individual driver sidecars will request scoped FS access later.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-open",
    "dialog:default",
    "stronghold:default",
    "deep-link:default",
    "deep-link:allow-on-open-url",
    "updater:default",
    "updater:allow-check",
    {
      "identifier": "core:webview:allow-create-webview-window",
      "allow": []
    }
  ]
}
